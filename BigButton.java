import javax.swing.*;

public class BigButton extends JButton {
    public BigButton() {
        setSize(300, 50);
    }
    public BigButton(ImageIcon icon) {
        setSize(300, 50);
        setIcon(icon);
    }
}
